# T5b SMOKE — ONE Leg-B closure mock THROUGH the MF forward + C_emu floor.
# n=1 PROFILING run; NOT a coverage verdict. Production NUTS is dense-mass, mtd=10,
# SLURM-only (~hours/mock). This smoke caps mtd to bound leapfrogs/sample.
# sim: ns0.803Ap2.2e-09herei4.05heref2.67alphaq2.21hub0.735omegamh20.141hireionz7.17bhfeedback0.056
# legs: DESI(n_z=11,N=681,floor_on=False), KS(n_z=12,N=72,floor_on=True)
# MF=ON  floor=ON (LF→HR generalization + n_s-edge, KS leg only)

## NUTS config
  n_warmup=60  n_samples=60  num_chains=1
  dense_mass=True  max_tree_depth=5  target_accept=0.9
  dense-mass DIMENSION = θ9(9) + τ₀-ladder(13) + α(3) = 25

## profiling
  wall-clock (sampling+warmup, 1 chain): 83.5 s
  divergences: 0 / 60  (rate 0.0%)
  max_tree_depth hits: 60 / 60  (mean depth 5.00, max 5)
  mean accept_prob: 0.986
  total leapfrog steps (sampling phase): 1860  (mean 31.0 leapfrogs/sample)
  per-leapfrog-step time (sampling-phase est): 22.5 ms

## ESS + ESS/sample (A_p, n_s, τ₀ at z≈3.0)
  n_s : ESS=18.1   ESS/sample=0.302
  A_p : ESS=21.3   ESS/sample=0.354
  τ₀  : ESS=59.7   ESS/sample=0.995  (z=3.00)

## recovered posterior mean±sd vs mock truth (n=1; NOT a coverage verdict)
  n_s : 0.8075 ± 0.0025   truth 0.8033   (+1.67σ)
  A_p : 1.887e-09 ± 7.102e-11   truth 2.203e-09   (-4.46σ)
  τ₀  : 0.4035 ± 0.0036   truth 0.4052   (-0.46σ)  (z=3.00)

## EXTRAPOLATION to production coverage (assumptions stated)
  smoke min-ESS over (A_p,n_s,τ₀) = 18.1; wall/ESS = 4.61 s/eff-sample
  ASSUMPTIONS: (1) production targets ESS≈400 per param (Stan rule);
    (2) production uses dense_mass + max_tree_depth=10 vs smoke mtd=5
        → leapfrogs/sample inflate by tree_factor≈4.71 (sub-exponential;
        the dense mass conditions the geometry so trees rarely saturate post-warmup);
    (3) 1 chain, CPU, wall≈CPU-h (single core); (4) warmup cost ≈ included in wall/ESS.
  per-mock @ production ≈ 2.41 CPU-h
    → N=99  mocks ≈ 239 CPU-h   (10.0 core-days)
    → N=600 mocks ≈ 1448 CPU-h   (60.4 core-days)
  NOTE: SLURM-only; embarrassingly parallel over mocks (fold_in seeding → array job).

## SCOPE (n=6 floor discipline)
  floor covers LF→HR GENERALIZATION only; HR→truth is the locked k<0.06 cap.
  ns>0.98 posterior rests on MF-correction EXTRAPOLATION (sigma_edge inflates C_emu;
  the (z,τ₀) interaction is untested there) — treat as GUARDED, not nominal.
